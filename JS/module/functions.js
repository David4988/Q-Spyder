
export const add = (x,y) => x+y;
export const sub = (x,y) => x-y;
export const mult = (x,y) => x*y;
export const div = (x,y) => x/y

// export const operation = (x,y,op) => {
//     oper = op(x,y)
//     return oper
// }

export default (x) => x*x